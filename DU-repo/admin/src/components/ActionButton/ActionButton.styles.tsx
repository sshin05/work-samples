import styled from '@emotion/styled';
import { Button, typography } from '@digital-u/digital-ui';

export const StyledActionButton = styled(Button)`
  font-weight: ${typography.weights.medium};
  line-height: 21px;
` as typeof Button;
