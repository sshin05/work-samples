import { css } from '@cerberus/styled-system/css';

export const sliderStyles = css({
  display: 'block',
  ml: 'auto',
  mr: 'auto',
  w: 'full'
});

export const hiddenInputButtonStyles = css({
  visibility: 'hidden',
  position: 'absolute'
});
