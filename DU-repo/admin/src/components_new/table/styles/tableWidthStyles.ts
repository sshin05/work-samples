import { css } from '@cerberus/styled-system/css';
import { vstack } from '@cerberus/styled-system/patterns';

export const tabsFullWidth = css({
  w: 'full',
  minW: '39rem'
});

export const tableFullWidth = vstack({
  gap: 0,
  w: 'full',
  minW: '39rem'
});
