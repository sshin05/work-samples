import { ServerPageLoader } from '@/components_new/loaders/ServerPageLoader';

const Loading = () => {
  return <ServerPageLoader header="Loading Preview..."></ServerPageLoader>;
};

export default Loading;
