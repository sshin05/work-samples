import { css } from '@cerberus/styled-system/css';

export const doughnutStyles = css({
  maxW: '300px',
  maxH: '10rem'
});

export const doughtnutLinkText = css({
  color: 'action.bg.initial',
  textDecoration: 'underline',
  display: 'inline',
  cursor: 'pointer'
});
